{% extends 'base.html.twig' %}

{% block title %} Edition de la demande{% endblock %}

{% block body %}
    {% block content %}
        <div class="container">
            <h1>- Edition de l'annonce -</h1>
            <h3>{{ demand.title }}</h3>
            <hr>

            {{ form_start(form) }}

            {{ form_widget(form) }}

            <button type="submit" class="btn btn-primary">Enregistrer les modifications</button>
            {{ form_end(form) }}


        </div>

    {% endblock %}
{% endblock %}
